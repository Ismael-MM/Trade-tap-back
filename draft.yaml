
models:
  User2:
    relationships:
      morphTo: Userable

  Trabajador:
    cif: string
    descripcion: text
    relationships:
      morphMany: User
      hasOne: HorarioTrabajador,HorarioInhabilitado
      belongsToMany: Profesion,Cliente
      hasMany: Solicitud
      
  Cliente:
    relationships:
      belongstoMany: Trabajador
      morphMany: User
      hasMany: Solicitud

  HorarioTrabajador:
    rango: enum:Lunes-Viernes,Lunes-Sábado,Lunes-Domingo
    hora_comienzo: timestamp
    hora_final: timestamp
    trabajador_id: unsignedInteger
    relationships:
      belongsTo: Trabajador

  HorarioInhabilitado:
    fecha: date
    relationships:
      belongsTo: Trabajador

  Profesion:
    nombre: string
    familia_profesional: string
    relationships:
      belongsToMany: Trabajador

  Solicitud:
    descripcion: text
    titulo: string
    estado: enum:Acceptado,Pendiente,Rechazado
    relationships:
      belongsTo: Trabajador,Cliente

seeders: 